package com.example.soundified2.HelpingScripts

object PlaylistData {
    //Lista en la que se irán almacenando todas las playlist que creemos.
    public var list:MutableList<Playlist> = mutableListOf<Playlist>(

    )
}